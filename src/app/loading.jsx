import React from "react";

const Loading = () => {
  return (
    <div className={"flex justify-center"}>
      <img src="spinner.sbg" alt="loading..." />
    </div>
  );
};

export default Loading;
